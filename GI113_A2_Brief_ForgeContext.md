# Assignment #2 — The Forge: โปรแกรมหลอมและสลายแร่

**วิชา GI113 Computer Programming (GI)** · ประกาศสัปดาห์นี้ (Week 6) · ส่งภายในสัปดาห์ที่ 7 · เนื้อหา: if / else if / else / Nested if / `&&` `||` (Week 6) + TryParse (Week 4) + Operators (Week 5) + `const` (Week 3)

---

## 🗺️ เริ่มยังไง

1. สร้าง Console App (C#) ชื่อโปรเจกต์ **`Assignment02`** ไว้ใน repo ที่ clone มา (คนละโฟลเดอร์ย่อยกับ `Assignment01` และ `Lab06`) — เตือน!!! ติ๊ก "Place solution and project in the same directory" ก่อนกด Create
2. วางเนื้อหาจาก `HEADER-TEMPLATE.txt` ไว้บรรทัดบนสุดของ `Program.cs` เหนือทุกอย่าง (แบบเดียวกับทุก Lab และ Assignment #1)

---

## 🎮 โจทย์

ทำโปรแกรม **โรงตีเหล็ก (Forge)** ของเกมที่แปลง **วัตถุดิบ (Ore)** เป็น **ของหลอม (Ingot)** และแปลงกลับได้ โปรแกรมทำงานรอบเดียวแล้วจบ:

1. แสดงหัวโปรแกรม และอัตราของวัสดุที่เลือก (Smelt / Salvage)
2. แสดงเมนู 2 รายการ: **S** สำหรับ Smelt, **B** สำหรับ Breakdown
3. รับ **menu** จากผู้ใช้ แล้วรับ **amount** (จำนวนที่ต้องการแปลง)
4. คำนวณและแสดงผลลัพธ์ พร้อมหน่วยที่ถูกต้อง — ถ้าพิมพ์ผิด ต้องไม่ crash และต้องบอกว่าผิดที่ไหน

### Smelt / Breakdown ดูจากมุมของ "ผู้เล่น"

| เมนู | ผู้เล่นทำอะไร | ผู้เล่นให้ระบบ (amount) | ผู้เล่นได้รับ | สูตร |
|---|---|---|---|---|
| **S** — Smelt | หลอม **Ore** ให้เป็น **Ingot** | Ore | Ingot | `amount * SmeltRate` |
| **B** — Breakdown | สลาย **Ingot** กลับเป็น **Ore** | Ingot | Ore | `amount / SalvageRate` |

Rate ทุกตัวออกแบบเอง — ตัวอย่างที่ใช้ในโจทย์นี้คือ Iron: Smelt `0.2500` / Salvage `0.3000`

### เลือกวัสดุและ Rate ยังไง

- เกมนี้เป็นเกมของคุณเอง ไม่มีเรทจริงให้อ้างอิง — **ตั้งชื่อวัสดุและ Rate เอง** (เช่น Iron, Crystal, Wood) แต่ Rate ต้องอยู่ระหว่าง `0` ถึง `1` ทั้งคู่ (แปลงแล้วได้น้อยลงเสมอ ไม่ใช่มากขึ้น)
- **กติกาสำคัญ: `SmeltRate` ต้องน้อยกว่า `SalvageRate` เสมอ** ไม่งั้นผู้เล่นจะหลอมแล้วสลายกลับไปมาเพื่อเสกวัตถุดิบจากอากาศได้ (ดูตัวอย่างบั๊กใน Game Context)
- **ทดสอบก่อนด้วย Rate ตัวอย่างในโจทย์ (Iron Smelt 0.2500 / Salvage 0.3000)** เทียบกับตาราง Test Cases ด้านล่างให้ตรงทุกเคส แล้วค่อยเปลี่ยนเป็นวัสดุและ Rate ของคุณ — วิธีนี้ทำให้รู้ทันทีว่าผิดที่โค้ดหรือที่ตัวเลข

### ที่ออกแบบเองได้

วัสดุและ Rate (ในกติกาข้างบน), ข้อความ error (ต้องเป็นภาษาอังกฤษ และบอกชัดว่าผิดที่ menu หรือ amount), หน้าตาของหัวโปรแกรม, ค่า `MaxBatch` (จำนวนสูงสุดต่อรอบ) ส่วนสิ่งที่ล็อกไว้คือ **concept ในตารางถัดไป** กับรูปแบบบรรทัดผลลัพธ์ตามตัวอย่าง

---

## ✅ สิ่งที่ต้องมีอยู่ในโค้ด

ต้องมีครบทุกแถว และต้อง **ใช้จริงในตำแหน่งที่ถูก** ไม่ใช่ใส่ให้ครบ:

| Concept | ต้องมีอะไร |
|---|---|
| **`if` / `else if` / `else`** | เช็ค menu: S → else if B → else (menu ไม่ถูกต้อง) |
| **Nested if** | การเช็ค menu ต้องอยู่ *ข้างใน* เงื่อนไขที่เช็ค amount ผ่านแล้ว (`if` ซ้อน `if`) |
| **`&&`** | เงื่อนไขของ amount ที่ถูกต้อง: parse ได้ **และ** มากกว่า 0 **และ** ไม่เกิน `MaxBatch` |
| **`\|\|`** | รับทั้งตัวพิมพ์เล็กและใหญ่: `s` หรือ `S`, `b` หรือ `B` — ห้ามใช้ `ToUpper()` / `ToLower()` แทน |
| **TryParse** | `double.TryParse` สำหรับ amount และ `char.TryParse` สำหรับ menu — ห้ามใช้ `Parse` หรือ `Convert` (พิมพ์ `SMELT` หรือกด Enter เปล่าแล้ว crash = ไม่ผ่านข้อนี้) |
| **`const`** | ประกาศอย่างน้อย 4 ตัว: ชื่อวัสดุ, `SmeltRate`, `SalvageRate`, `MaxBatch` (PascalCase) |
| **ปีกกา** | ทุก `if` / `else if` / `else` ต้องมี `{ }` แม้มีบรรทัดเดียว |
| **ห้ามใช้** | `switch` (เรียนสัปดาห์หน้า), loop (ยังไม่ได้เรียน) — โจทย์นี้ต้องเป็น `if` ล้วน |
| **Output** | ภาษาอังกฤษล้วน (console ของ Windows แสดงไทยเพี้ยน), ตัวเลขผลลัพธ์ทศนิยม 2 ตำแหน่ง |

### Logic ที่ต้อง implement (เขียนเป็นภาษาคน ไม่ใช่โค้ด)

```
อ่าน menu   (char.TryParse)
อ่าน amount (double.TryParse)

ถ้า amount ถูกต้อง (parse ได้ และ 0 < amount <= MaxBatch)
    ถ้า menu คือ S หรือ s        → amount * SmeltRate    → แสดงเป็น Ingot
    ไม่งั้นถ้า menu คือ B หรือ b   → amount / SalvageRate  → แสดงเป็น Ore
    ไม่งั้น                        → แสดงว่า menu ไม่ถูกต้อง
ไม่งั้น                            → แสดงว่า amount ไม่ถูกต้อง
```

---

## 🧪 Test Cases (ใช้ Rate ตัวอย่าง Iron Smelt 0.2500 / Salvage 0.3000)

ตารางนี้ไว้ **เช็คว่าโค้ดของคุณถูกแล้วหรือยัง** ก่อนเปลี่ยนเป็นวัสดุและ Rate ของคุณเอง — ผลลัพธ์มาจากโปรแกรมอ้างอิงที่คอมไพล์และรันจริงแล้ว บรรทัดผลลัพธ์ต้องตรงกับตารางนี้เมื่อใช้ Rate ตัวอย่าง (ข้อความ error เขียนเองได้):

| # | พิมพ์ menu | พิมพ์ amount | ผลลัพธ์ที่ต้องได้ |
|---|---|---|---|
| 1 | `S` | `100` | `=> 100.00 Iron Ore = 25.00 Iron Ingot` |
| 2 | `s` | `4` | `=> 4.00 Iron Ore = 1.00 Iron Ingot` |
| 3 | `B` | `25` | `=> 25.00 Iron Ingot = 83.33 Iron Ore` |
| 4 | `b` | `1` | `=> 1.00 Iron Ingot = 3.33 Iron Ore` |
| 5 | `S` | `500` | `=> 500.00 Iron Ore = 125.00 Iron Ingot` (ขอบบนพอดี ต้องผ่าน) |
| 6 | `S` | `500.01` | error: amount (เกินขอบบน) |
| 7 | `S` | `0` | error: amount (ไม่มากกว่า 0) |
| 8 | `S` | `abc` | error: amount (parse ไม่ได้) |
| 9 | `X` | `100` | error: menu |
| 10 | `SMELT` | `100` | error: menu (`char.TryParse` ไม่ผ่าน ต้องไม่ crash) |

ตัวอย่างการรันเคส 1 (ค่าท้ายบรรทัด `Choose Menu:` และ `How much would you like:` คือสิ่งที่ผู้ใช้พิมพ์):

```
-----------------------------------
--     Welcome to the Forge      --
-----------------------------------
=> Iron Smelting 0.25 / Salvage 0.3
=> Key 'S' for Smelt (Ore -> Ingot)
=> Key 'B' for Breakdown (Ingot -> Ore)
=> Choose Menu: S
=> How much would you like: 100
=> 100.00 Iron Ore = 25.00 Iron Ingot
```

> หัวโปรแกรมและข้อความเมนูเป็นแค่ตัวอย่าง ออกแบบใหม่ได้ ส่วนบรรทัดผลลัพธ์ให้ยึดรูปแบบ `=> <amount> <หน่วยที่ให้> = <ผลลัพธ์> <หน่วยที่ได้>`

---

## 🎮 Game Context

เกม crafting ที่มีวัตถุดิบดิบกับของหลอม (เช่น Minecraft ที่ ore กับ ingot, หรือเกมที่แปลง gem หยาบเป็น gem เจียระไน) ต้องมี "ตัวแปลง" แบบนี้ทั้งหมด และอัตราต้องถูกต้อง 100% — **ถ้า `SmeltRate` มากกว่าหรือเท่ากับ `SalvageRate`** ผู้เล่นจะหลอมแล้วสลายกลับไปมาซ้ำๆ เพื่อเสกวัตถุดิบจากอากาศได้ (ทดสอบจริงแล้ว: ตั้ง SmeltRate = 0.30 และ SalvageRate = 0.25 เอา ore 100 หน่วยหลอมเป็น ingot 30 แล้วสลายกลับได้ ore 120 หน่วย — ได้กำไร 20 หน่วยจากอากาศ) ซึ่งเป็นบั๊ก Economy ในเกมที่คลาสสิกมาก และถ้าผู้เล่นพิมพ์อะไรแปลกๆ แล้วเกม crash ก็เป็นบั๊กที่ทีมต้องแก้ทันที

---

## ✅ สิ่งที่ต้องส่ง

1. **Header comment** (Student ID / Name / Section / No.) อยู่บรรทัดบนสุดของ `Program.cs`
2. โปรแกรมต้อง compile และรันได้จริง ไม่มี error
3. ครบทุกแถวในตาราง "สิ่งที่ต้องมีอยู่ในโค้ด" และ `SmeltRate` น้อยกว่า `SalvageRate` ตามกติกา
4. Commit ด้วยข้อความที่สื่อความหมายจริง (ห้ามใช้คำว่า `"update"`) แล้ว push ขึ้น repo ตัวเอง `GI113-2026-<รหัสนักศึกษา>` ตรวจด้านในโฟลเดอร์ชื่อ `Assignment02`
5. ส่ง **ลิงก์ commit** และ **screenshot ผลลัพธ์การรันจริง** อย่างน้อย 3 รอบ: รอบที่เลือก S, รอบที่เลือก B และรอบที่พิมพ์ผิด 1 แบบ (ถ้ายังไม่เคยส่งงานลิงก์ Git ให้แจ้งอาจารย์ได้แล้ว)

## 📊 เกณฑ์การให้คะแนน (5 ข้อ × 1 คะแนน)

อาจารย์จะอ่านโค้ด + รันจริงด้วย input ในตาราง Test Cases + ดู screenshot ประกอบกัน

| # | เกณฑ์ | ดูอะไร |
|---|---|---|
| 1 | คำนวณถูก หน่วยถูก | Smelt ใช้ `*` กับ `SmeltRate` ได้ Ingot, Breakdown ใช้ `/` กับ `SalvageRate` ได้ Ore, ป้ายหน่วยตรง, ทศนิยม 2 ตำแหน่ง, ผลตรงกับ Test Cases, `SmeltRate` < `SalvageRate` |
| 2 | โครงสร้างเงื่อนไข | มี if / else if / else, Nested if, `&&`, `\|\|` ครบและใช้ถูกตำแหน่งตามตาราง |
| 3 | Input Validation | TryParse ทั้งสอง input, ขอบเขต amount ถูกต้อง (0 ไม่ผ่าน, 0.01 ผ่าน, MaxBatch ผ่าน, เกิน MaxBatch ไม่ผ่าน), พิมพ์อะไรมาก็ไม่ crash |
| 4 | คุณภาพโค้ดและ output | `const` PascalCase, ตัวแปรใน `Main` camelCase ชื่อสื่อความหมาย, ปีกกาทุก `if`, output ภาษาอังกฤษ, header comment ครบ, ไม่มี `switch` / loop |
| 5 | Commit, Push & Screenshot | Commit message สื่อความหมาย, push ตรงโฟลเดอร์ `Assignment02`, screenshot ≥ 3 รอบตรงกับโค้ดที่ push จริง |

---

## 💡 คำแนะนำ

- **ตรวจก่อนส่ง 4 ข้อที่พลาดกันบ่อยที่สุด:**
  1. Breakdown ต้อง **หาร** ไม่ใช่คูณ (Smelt คูณ, Breakdown หาร) — ลองประมาณด้วยหัวก่อนรัน: ถ้า Rate ทั้งคู่น้อยกว่า 1 แล้วสลับสูตรกัน ตัวเลขจะดูผิดปกติทันที
  2. `SmeltRate` ต้อง **น้อยกว่า** `SalvageRate` เสมอ (ทดสอบแล้วว่าถ้าสลับกัน ผู้เล่นเสกวัตถุดิบจากอากาศได้) — ก๊อปบรรทัดมาวางแล้วลืมแก้เลขเป็นบั๊กที่เจอบ่อย
  3. ป้ายหน่วยสลับ: Smelt ผลลัพธ์เป็น Ingot, Breakdown ผลลัพธ์เป็น Ore
  4. ใช้ `=` แทน `==` ในเงื่อนไข (Compiler จะฟ้อง error CS0029 "Cannot implicitly convert type ... to 'bool'" ให้อ่านตรงนั้นแล้วแก้)
- `TryParse` ที่ล้มเหลวจะทำให้ตัวแปรที่ `out` ออกมาเป็นค่าเริ่มต้น (`double` เป็น `0`, `char` เป็น `'\0'`) — คิดดูว่าค่านั้นไปตกกิ่งไหนของ `if` ของคุณ
- แสดงทศนิยม 2 ตำแหน่งใน string interpolation: `{ตัวแปร:F2}` เช่น `$"{ingot:F2}"`
- เช็คให้ครบด้วยการรัน Test Cases ทีละเคส — ไม่ต้องเชื่อว่าโค้ด "น่าจะถูก"
- ธีมหรือวัสดุซ้ำกับเพื่อนได้ไม่ผิดกติกา แต่โครงโค้ด ชื่อตัวแปร และข้อความควรเป็นของตัวเอง — ถ้าโค้ดเหมือนเพื่อนเป๊ะๆ ขออนุญาต 0 คะแนนทั้งสองคนนะครับ
